#!/bin/bash

pane_id=$1

# pane_pid=$(tmux list-panes -F "#{pane_pid}")

# echo "$pane_pid"
echo "$pane_id"

# python_env=$(which python | sed -n 's|.*/\([^/]*\)/bin/python|(\1)|p')
#
# echo "$python_env"

